import type { maybe } from './maybe';
/**
 * Returns the first falsy or last item function call or value in `in_value_a1` .
 */
export declare function and_fn<I extends unknown, O extends unknown>(a1_nowrap: I): maybe<O>;
export declare const and__fn: typeof and_fn;
