import type { wrap_a1_type } from './wrap_a1_type';
/**
 * Returns `!=` operator to all values in `in_value_a1`.
 */
export declare function neq<I extends unknown>(in_value_a1: wrap_a1_type<I>): boolean;
