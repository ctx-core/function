/**
 * Return function that Returns `!=` operator to all values in `in_value_a`.
 */
export declare function neq_<I extends unknown = unknown>(in_value_a: I | I[]): (val_a: I) => boolean;
export { neq_ as _neq, };
