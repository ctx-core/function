import type { falsy } from './_types'
/**
 * Returns the first falsy or last item function call or value in `in_value_a` .
 */
export declare function and_<I extends unknown = unknown, O extends unknown = I>(a_nowrap: I): O | falsy;
export { and_ as and__fn };
