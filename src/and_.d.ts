import type { falsy } from './falsy';
/**
 * Returns the first falsy or last item function call or value in `in_value_a` .
 */
export declare function and_</*@formatter:off*/ I extends unknown = unknown, O extends unknown = unknown>(a_nowrap: I): O | falsy;
export { and_ as and__fn };
