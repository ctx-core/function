/**
 * Returns function that returns `===` operator to all values in `in_value_a`.
 */
export declare function eql_<Val extends unknown = unknown>(in_value_a:Val[]):(val:Val)=>boolean;
export { eql_ as _eql, }
