import type { maybe } from './maybe';
/**
 * Returns the first falsy or last item function call or value in `in_value_a1` .
 */
export declare function and_fn</*@formatter:off*/ I extends unknown, O extends unknown>(a1_nowrap: I): maybe<O>;
export { and_fn as and__fn };
