import type { wrap_a_T } from './wrap_a_T';
/**
 * Returns `!=` operator to all values in `in_value_a`.
 */
export declare function neq<I extends unknown = unknown>(in_value_a: wrap_a_T<I>): boolean;
