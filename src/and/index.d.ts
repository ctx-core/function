import type { a_nowrap_T, falsy } from '../_types'
/**
 * Returns the first falsy or last item.
 */
export declare function and<I extends unknown = unknown, O extends unknown = I>(a_nowrap:a_nowrap_T<I>):O|falsy;
